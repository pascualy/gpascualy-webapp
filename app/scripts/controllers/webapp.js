'use strict';

/**
 * @ngdoc function
 * @name gpascualyWebappApp.controller:WebappCtrl
 * @description
 * # WebappCtrl
 * Controller of the gpascualyWebappApp
 */
angular.module('gpascualyWebappApp')
  .controller('WebappCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
